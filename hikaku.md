# VRAMDISK / GpuRamDrive / RAM ディスク 比較

メモリをドライブレターとして見せる 3 つの実装を、**同一マシン・同一ハーネス**で計測した結果。
再現手順は [`scripts/bench_three_way.ps1`](scripts/bench_three_way.ps1)。

```
pwsh -File scripts\bench_three_way.ps1 -RamDisk R:\ -GpuRamDrive P:\ -Drive V:
```

---

## 1. 比較対象

| | 実体 | ファイルシステム | 経路 |
|---|---|---|---|
| **RAM ディスク** | システム RAM | NTFS | カーネルのブロックデバイス（ImDisk 2.1.1.50） |
| **GpuRamDrive** | GPU VRAM | NTFS | 同じブロックデバイス + ユーザーモードプロキシ → PCIe（v0.4, CUDA ビルド） |
| **VRAMDISK** | GPU VRAM | 自前（WinFsp） | ユーザーモードファイルシステム → PCIe |

3 つの違いは「どこにバイトを置くか」だけではない。GpuRamDrive は ImDisk のプロキシとして
**ブロックデバイス**を提供し、その上に Windows が NTFS を載せる。VRAMDISK は
**ファイルシステムそのもの**がプロセス内にあるので、GPU がデータをその場で計算できる。
この構造差が、後半の「データ処理」の表にそのまま出る。

### 計測環境

```
CPU     : Intel Core i9-12900K (16C/24T)
RAM     : 127.7 GB
GPU     : NVIDIA GeForce RTX 4070 (12282 MiB) / driver 591.86 / PCIe 4.0 x16
OS      : Windows 11 Pro build 22635
WinFsp  : 2025 / ImDisk : 2.1.1.50 / GpuRamDrive : v0.4 (CUDA)
pwsh    : 7.4.14
```

ボリューム: `R:` 30 GB NTFS（ImDisk）、`P:` 4 GB NTFS（GpuRamDrive）、`V:` 4 GiB（VRAMDISK）。
シーケンシャルは 2 GiB ファイル、圧縮は 512 MiB のログ様テキスト、小ファイルは 4000 × 4 KiB。
各値は 3 回の**最良値**。3 者を同一プロセス・同一セッションで連続計測している。

---

## 2. 要約

| 観点 | 勝者 | 差 |
|---|---|---|
| 大ブロック書き込み | **VRAMDISK** | 6.46 GB/s vs RAM 2.61 / GRD 1.19（buffered 16 MiB） |
| 大ブロック読み取り | **RAM ディスク** | 8.35 GB/s vs VRAMDISK 3.52 / GRD 1.65（unbuffered 16 MiB） |
| 小ブロック・メタデータ | **RAM ディスク** | create/s で VRAMDISK の 5.8 倍 |
| レイテンシ | **RAM ディスク** | 4 KiB ランダム読み 11.9 µs vs 36.2 / 41.0 |
| 並列読み取りのスケール | **RAM ディスク** | 8 スレッドで 8.64 GB/s、GPU 勢は 1.3〜1.6 で頭打ち |
| **全文検索** | **VRAMDISK** | 20.3 GB/s vs 6.1 / 5.0（連続配置なら 100 GB/s 超） |
| **zip 圧縮** | **VRAMDISK** | 0.21 s vs 1.20 / 1.22（5.7 倍） |
| ハッシュ | 引き分け | 逐次アルゴリズムなので GPU の出番がない |
| ホスト RAM 消費 | **GPU 勢** | 2 GiB のデータを置いてもホスト RAM は増えない |

**GPU 上の 2 実装だけを比べると、VRAMDISK が GpuRamDrive を全項目で上回る。**
RAM ディスクとは勝ち負けが分かれ、「何をするドライブか」で選ぶことになる。

---

## 3. シーケンシャル I/O

2 GiB ファイル、3 回の最良値、GB/s。`unbuffered` は `FILE_FLAG_NO_BUFFERING`
（ストレージ経路そのもの）、`buffered` は通常のアプリケーション（Windows キャッシュマネージャ経由）。
両方載せているのは、3 者ともキャッシュマネージャの後ろにいるため、片方だけではデバイスではなく
キャッシュを測ってしまうから。

### unbuffered

| ブロック | RAM disk W / R | GpuRamDrive W / R | VRAMDISK W / R |
|---|---|---|---|
| 4 KiB | 0.20 / 0.46 | 0.06 / 0.11 | 0.09 / 0.11 |
| 64 KiB | 2.49 / 3.39 | 0.71 / 0.86 | **1.32 / 1.17** |
| 1 MiB | 7.24 / 7.27 | 1.42 / 1.41 | **6.10 / 2.27** |
| 16 MiB | 5.82 / 8.35 | 1.47 / 1.65 | **6.43 / 3.52** |

### buffered

| ブロック | RAM disk W / R | GpuRamDrive W / R | VRAMDISK W / R |
|---|---|---|---|
| 4 KiB | 1.39 / 2.78 | 0.84 / 2.26 | 0.10 / 0.10 |
| 64 KiB | 2.56 / 7.18 | 1.18 / 5.81 | 1.07 / 1.06 |
| 1 MiB | 2.73 / 7.34 | 1.23 / 5.84 | **6.12** / 2.32 |
| 16 MiB | 2.61 / 6.24 | 1.19 / 4.81 | **6.46** / 3.52 |

読み取れること。

- **大ブロック書き込みは VRAMDISK が最速**。unbuffered 16 MiB で 6.43 GB/s、RAM ディスクの
  5.82 GB/s を上回り、GpuRamDrive の 4.4 倍。ホスト→デバイスのコピーが PCIe の実効帯域に
  張り付いているため（後述の生バンド幅で H2D 10.89 GB/s）。
- **読み取りは書き込みより遅い**。これは PCIe の非対称性で、生バンド幅でも D2H は H2D の
  半分以下（64 MB で 8.08 vs 10.89 GB/s）。VRAMDISK 3.52 GB/s に対し RAM ディスクは 8.35 GB/s。
- **buffered の小ブロックが弱点**。4 KiB で 0.10 GB/s と、RAM ディスクの 1/14。
  ブロックごとに WinFsp のユーザーモード往復が入るため。NTFS を載せている 2 者は
  キャッシュマネージャが要求をまとめてくれるので、ここで大きく差が付く。
- GpuRamDrive の buffered 読み取りが 5.8 GB/s と速いのは、NTFS + キャッシュマネージャが
  効いているから。unbuffered にすると 1.41 GB/s まで落ち、これが実際のデバイス速度。

---

## 4. ランダム読み取りレイテンシ（unbuffered、µs/op）

| ブロック | RAM disk | GpuRamDrive | VRAMDISK |
|---|---|---|---|
| 4 KiB | **11.9** | 41.0 | 36.2 |
| 64 KiB | **23.0** | 88.2 | 47.2 |
| 1 MiB | **137.6** | 944.6 | 588.0 |

RAM ディスクが 3〜7 倍速い。PCIe 往復のレイテンシは物理的に消せないので、これは
GPU をストレージにする限り付いて回るコストである。GPU 勢どうしでは VRAMDISK が
GpuRamDrive より一貫して速い（1 MiB で 1.6 倍）。

---

## 5. 並列シーケンシャル読み取り（1 MiB、unbuffered、GB/s）

| スレッド | RAM disk | GpuRamDrive | VRAMDISK |
|---|---|---|---|
| 1 | 6.96 | 1.44 | 1.54 |
| 2 | 7.97 | 1.28 | 1.57 |
| 4 | 8.45 | 1.37 | **1.84** |
| 8 | **8.64** | 1.33 | 1.59 |

**GPU 勢はどちらもスケールしない。** RAM ディスクが 1 → 8 スレッドで 1.24 倍に伸びるのに対し、
VRAMDISK は 1.54 → 1.59 でほぼ横ばい（4 スレッドの 1.84 が最大）、GpuRamDrive はむしろ下がる。
複数の読み手が同じ PCIe リンクを共有するため、リンクが飽和した時点でそれ以上増えない。

なおエンジン単体（ファイルシステムを介さない）では 8 スレッド 1 MiB で
排他 7.00 GB/s → 共有 9.75 GB/s と 1.39 倍にスケールしており、頭打ちの原因は
エンジンのロックではなく転送経路側にある。

---

## 6. データ処理 — 設計差が出るところ

検索・ハッシュは 2 GiB、zip は 512 MiB のログ様テキスト。
RAM ディスクと GpuRamDrive は**ただのブロックデバイス**なので、同じ仕事をするには
全バイトを CPU に運ぶしかない。その列は手抜き実装ではなく、入手できる最良の CPU 実装
（.NET のベクトル化された `Span<byte>.IndexOf`、`SHA256`、`Deflate`）である。

| 処理 | RAM disk | GpuRamDrive | VRAMDISK | 備考 |
|---|---|---|---|---|
| 全文検索 2 GiB | 0.33 s / 6.12 GB/s | 0.40 s / 5.04 GB/s | **0.10 s / 20.29 GB/s** | GPU カーネル。3.3〜4.0 倍 |
| zip 圧縮 512 MiB | 1.20 s / 0.42 GB/s | 1.22 s / 0.41 GB/s | **0.21 s / 2.34 GB/s** | nvCOMP deflate。5.7 倍 |
| SHA-256 2 GiB | **1.17 s / 1.71 GB/s** | 1.24 s / 1.61 GB/s | 1.85 s / 1.08 GB/s | 逐次アルゴリズム。GPU の負け |
| MD5 2 GiB | 3.22 s / 0.62 GB/s | 3.35 s / 0.60 GB/s | **3.17 s / 0.63 GB/s** | 同上、横並び |

**ハッシュで GPU が勝てないのは仕様どおり**である。SHA-256 も MD5 も直前のブロックの結果を
次のブロックが必要とするので、1 ファイルの中に並列性がない。VRAMDISK のキャリブレーションは
これを計測して CPU に振り分けており、GPU に流していれば 30 倍遅くなる。
SHA-256 で VRAMDISK が 1.85 s とやや遅いのは、ジョブ制御面（記述子の書き込み、状態の
ポーリング、結果 JSON）のオーバーヘッドが乗るため。

zip の出力サイズは VRAMDISK が 21 MiB、CPU 側が 17 MiB。nvCOMP の deflate は
圧縮率で .NET に劣る代わりに 5.7 倍速い、というトレードオフになっている。

### 全文検索の詳細

検索カーネル単体（データが既に VRAM にある状態、`vramdisk cli --bench`）:

| パターン | CPU スキャン | GPU スキャン | 倍率 | ヒット数 |
|---|---|---|---|---|
| rare (14 B) | 4.09 GB/s | **196.41 GB/s** | 48.0x | 1 |
| common (10 B) | 2.24 GB/s | **98.80 GB/s** | 44.1x | 7,354,396 |
| short (3 B) | 2.71 GB/s | **105.61 GB/s** | 38.9x | 7,354,396 |

ジョブ API 経由で 1 ファイルを検索したときのスケール（連続配置のファイル、ウォーム後の最良値）:

| ファイルサイズ | エンジン内 | 壁時計 | 壁時計 GB/s |
|---|---|---|---|
| 1 MiB | 0 ms | 7.3 ms | 0.13 |
| 16 MiB | 0 ms | 10.3 ms | 1.51 |
| 128 MiB | 5 ms | 17.3 ms | 7.23 |
| 512 MiB | 5 ms | 20.1 ms | 24.93 |
| 2048 MiB | 11 ms | 19.3 ms | **103.69** |

ジョブ 1 件あたり **10〜20 ms の固定オーバーヘッド**（記述子の受け渡しとポーリング）があるため、
小さいファイルでは意味がなく、大きいほど有利になる。

**初回だけ 5 秒かかる。** マウント後の最初の検索ジョブは NVRTC が検索カーネルをコンパイルする。
実測: 1 回目 5020 ms、2 回目以降 10〜11 ms（10 連続実行で安定）。

---

## 7. 発見した性能上の欠陥：チャンクサイズでの上書きが配置を崩す

上の表で、ヘッドトゥヘッドの検索が 20.3 GB/s なのに、専用計測では 103 GB/s 出ている。
8 倍の差を推測で説明したくなかったので、原因を切り分けた。

| 仮説 | 検証方法 | 結果 |
|---|---|---|
| 初回のカーネルコンパイル | 同一マウントで 10 連続実行 | 1 回目 5020 ms、以降 10〜11 ms → **無関係** |
| VRAM の空き不足 | 4 GiB 中 2.5 GiB 使用状態で計測 | 11 ms → **無関係** |
| 持続負荷によるクロック低下 | 120 秒の連続 I/O 後に計測 | 10 ms（SM クロックは 2490 → 2865 MHz に上昇）→ **無関係** |
| ブロックサイズそのもの | 4 KiB / 64 KiB / 1 MiB で 1 回だけ書いて計測 | 5〜7 ms / 1 GiB（143〜200 GB/s）→ **無関係** |
| **同じファイルの上書き** | ブロックサイズ別に 3 回上書きして計測 | **64 KiB のときだけ 86 ms** |

```
before rewrites:                  engine 11 ms
after 3 rewrites at 4 KiB:        engine 11 ms
after 3 rewrites at 64 KiB:       engine 86 ms   <-- チャンクサイズと一致
after 3 rewrites at 1024 KiB:     engine 10 ms   <-- 回復する
after 3 rewrites at 16384 KiB:    engine 13 ms
```

`CHUNK_SIZE` はちょうど 64 KiB である。エンジンの `raw_runs()` は、論理的に隣り合うチャンクの
VRAM アドレスが連続している間だけ 1 つの「ラン」にまとめる。まとまれば 2 GiB を数回の
カーネル起動で走査できるが、崩れると 32,768 個のチャンクそれぞれにカーネル起動と
ホスト側の境界つなぎが要る。ちょうどチャンク 1 個分ずつの上書きを繰り返すと配置が
連続でなくなり、この経路に落ちる。

ベンチのシーケンシャル計測は同じファイルを 24 回上書きするので、検索の番になる頃には
この状態になっている。**したがって表の 20.3 GB/s は最悪ケース側の数字**であり、
通常の書き方をしたファイルでは 100 GB/s 超で走る。どちらも実測値なので両方載せた。

これは修正すべき欠陥である（未修正）。1 回だけ書いたファイルは影響を受けない。

---

## 8. メモリの確保モデル

「30 GB の RAM ディスク」と「4 GB の GPU ドライブ」は、確保の仕方が根本的に違う。

| | マウント時に確保 | 2 GiB 書き込み中 | 削除後 |
|---|---|---|---|
| RAM ディスク（30 GB） | ほぼ 0 | ホスト RAM **+1937 MiB** | **解放される**（+2 MiB） |
| GpuRamDrive（4 GB） | VRAM **+4242 MiB**、ホスト +150 MiB | 増えない | 解放されない |
| VRAMDISK（4 GiB） | VRAM **+4249 MiB**、ホスト +250 MiB | 増えない | 解放されない |

- **ImDisk の RAM ディスクは要求時確保**。30 GB と宣言しても、使うまでコストは実質ゼロで、
  ファイルを消せば返ってくる。宣言サイズを大きめに取っておける。
- **GPU 側の 2 実装はマウント時に全額確保**。空でも 4 GB 分の VRAM が消える。
  ゲームや推論と VRAM を取り合うので、サイズは慎重に決める必要がある。
- ただし**ホスト RAM は消費しない**。128 GB 積んでいても RAM ディスクに 30 GB 取れば
  その分アプリケーションが使える RAM は減るが、GPU 側なら減らない。VRAM が余っていて
  ホスト RAM が逼迫している環境では、これが決定的な利点になる。

---

## 9. メタデータ・小ファイル（4000 × 4 KiB）

| | create/s | stat/s | read/s | delete/s |
|---|---|---|---|---|
| RAM ディスク | **10,334** | 78,903 | **60,558** | **36,133** |
| GpuRamDrive | 5,640 | **90,618** | 41,612 | 29,303 |
| VRAMDISK | 1,774 | 19,962 | 12,811 | 13,307 |

**VRAMDISK が明確に負けている領域。** create/s で RAM ディスクの 1/5.8。
NTFS を載せている 2 者は成熟したカーネルモードのファイルシステムであるのに対し、
VRAMDISK は 1 操作ごとにユーザーモードへの往復が入る。`node_modules` の展開や
ビルド中間ファイルの置き場のような、小さなファイルを大量に作る用途には向かない。

（メタデータには 1000 ms のキャッシュを入れてあるため、`stat/s` の 19,962 は
キャッシュヒットを含む値である。それでもなお 4 倍の差がある。）

---

## 10. 参考：デバイス層の生の数字

VRAMDISK の内蔵ベンチ（`vramdisk cli --bench`）。上のファイルシステム層の数字が
どこまで理論値に近いかの目安になる。

**生の VRAM バンド幅（host ↔ device memcpy）**

| サイズ | Write (H→D) | Read (D→H) |
|---|---|---|
| 4 KB | 249.9 MB/s | 362.8 MB/s |
| 256 KB | 7.69 GB/s | 7.32 GB/s |
| 4 MB | 11.04 GB/s | 4.38 GB/s |
| 64 MB | **10.89 GB/s** | **8.08 GB/s** |

**ストレージエンジン（ファイルシステムを介さない）**

| ファイルサイズ | Write | Read |
|---|---|---|
| 1 MB | 9.80 GB/s | 3.01 GB/s |
| 16 MB | 7.26 GB/s | 5.09 GB/s |
| 64 MB | 8.98 GB/s | 5.54 GB/s |
| 256 MB | 9.64 GB/s | 4.79 GB/s |

ファイルシステム越しの 6.43 GB/s（16 MiB 書き込み）は、エンジンの 8.98 GB/s の 72%、
生の memcpy の 59% に相当する。残りが WinFsp の往復コストである。

**GPU ハッシュ（dedup 経路の FNV-1a、並列化できるケース）**

| | |
|---|---|
| バッチ（256 チャンク/起動） | **73.60 GB/s** |
| 単チャンク | 858.9 MB/s（72.8 µs/チャンク） |

並列性がある処理なら GPU は圧倒的に速い。SHA-256 が遅いのは GPU が遅いからではなく、
アルゴリズムが逐次だからである、ということがこの 2 行に出ている。

---

## 11. どれを選ぶか

**RAM ディスクを選ぶべき場合**

- 小さなファイルを大量に扱う（ビルドの中間生成物、`node_modules`、キャッシュ）
- レイテンシが効く（4 KiB ランダム読みで 3 倍速い）
- 並列アクセスが多い（唯一スケールする）
- ホスト RAM に余裕がある

**VRAMDISK を選ぶべき場合**

- **ホスト RAM を減らしたくないが VRAM は余っている**
- 大きなファイルを扱う（16 MiB ブロックの書き込みは最速）
- **置いたデータを検索・圧縮したい**。全文検索 3.3〜4 倍（連続配置なら 15 倍以上）、
  zip 5.7 倍。これは他の 2 つには原理的にできない
- ジョブ API で GPU 処理を仕込みたい（`$VRAMDISK\jobs` に記述子を置くだけ）

**GpuRamDrive を選ぶべき場合**

- 計測した範囲では、VRAMDISK に対する優位点は見当たらなかった。
  シーケンシャル・レイテンシ・並列・小ファイルの全項目で VRAMDISK が上回り、
  データ処理機能は持たない。ただし NTFS がそのまま載るので、
  ADS・EFS・NTFS 圧縮・ハードリンクなど**NTFS 固有の機能が必要なら**選択肢になる。

**共通の注意**

- 3 つとも**揮発性**である。電源断・クラッシュ・アンマウントで中身は消える。
  VRAMDISK はアンマウント時に zip として書き出す選択肢を用意している。
- GPU 側の 2 つは VRAM を先に丸ごと確保する。GPU を使う他のアプリケーションと競合する。

---

## 付録：測定方法

- ハーネス: [`scripts/bench_three_way.ps1`](scripts/bench_three_way.ps1)。3 者すべてを同一の
  P/Invoke ラッパ（`CreateFileW` / `ReadFile` / `WriteFile`）で駆動している。
  unbuffered 用のバッファは `VirtualAlloc` でページ境界に揃えている
  （`byte[]` ではセクタ境界が保証されないため）。
- 各測定は 3 回の最良値。最良値を採るのは、他プロセスの割り込みによる外れ値を
  除くためで、平均だと環境ノイズを測ってしまう。
- 並列読み取りは各スレッドが自分のハンドルで自分の区間を読む。全スレッドが
  `ManualResetEventSlim` で同時に走り出すよう揃えている。
- CPU 側の全文検索は .NET がベクトル化する `Span<byte>.IndexOf` を使い、
  バッファ間にまたがる一致を落とさないようオーバーラップを持たせている。
- 生の数字は `.bench_three_way.json` に出力される（git 管理外）。
- 本文の「run-to-run のばらつき」について: 同じ計測を 2 回通した際、
  unbuffered 1 MiB 書き込みで RAM ディスクが 12.15 → 7.24 GB/s と振れた。
  桁が変わるものではないが、1 割程度の差は誤差の範囲として読んでほしい。
  VRAMDISK と GpuRamDrive の相対関係は 2 回とも変わらなかった。
